const generateHTML = require('./src/generateHTML');
// node modules
const inquirer = require("inquirer");
const fs = require("fs");
// team profiles
const Engineer = require("./lib/Engineer");
const Intern = require("./lib/Intern");
const Manager = require("./lib/Manager");

// team array
const teamArray =[];
// start of Engineer prompts
const addEngineer = () => {
  
}